# Sign Language Production (SLP) - Installation and Usage Guide

## 🎯 مرحباً بك في Sign Language Production!

تطبيق ذكي مدعوم بالذكاء الاصطناعي للتعرف على الإشارات اليدوية (Sign Language) وتحويلها إلى نصوص في الوقت الفعلي.

---

## ✨ المميزات الرئيسية

✅ **التعرف الفوري** - تعرف على الإشارات في الوقت الفعلي من خلال الكاميرا  
✅ **دقة عالية** - نموذج تعلم عميق مدرب على مئات العينات  
✅ **واجهة سهلة** - تطبيق PyQt5 بسيط وسهل الاستخدام  
✅ **دعم متعدد** - يدعم 12 إشارة يدوية مختلفة  
✅ **قابل للتوسع** - يمكن إضافة إشارات جديدة بسهولة  

---

## 📋 المتطلبات

- **Python 3.9+**
- **pip** (مدير الحزم)
- **كاميرا ويب** (لالتقاط الفيديو)

---

## 🚀 التثبيت السريع

### 1. استنساخ المستودع
```bash
git clone https://github.com/adelaboelkhair-cloud/SLP.git
cd SLP
```

### 2. إنشاء بيئة افتراضية (اختياري لكن موصى به)
```bash
# على Windows
python -m venv venv
venv\Scripts\activate

# على Mac/Linux
python3 -m venv venv
source venv/bin/activate
```

### 3. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4. التثبيت المتقدم (اختياري)
```bash
pip install -e .
```

---

## 📚 الاستخدام

### الخطوة 1: جمع البيانات (Data Collection)

قبل تدريب النموذج، تحتاج إلى جمع بيانات الإشارات:

```bash
python collect_data.py
```

**التعليمات:**
- سيتم طلب منك بدء جمع البيانات لكل إشارة
- اضغط Enter للبدء
- اعرض الإشارة أمام الكاميرا واضغط 'c' لالتقاط العينة
- كرر حتى تجمع العدد المطلوب من العينات
- اضغط 'q' للخروج

**الإشارات المدعومة:**
- hello, thank_you, yes, no, good, bad, love, peace, stop, ok, help, sorry

### الخطوة 2: تدريب النموذج (Model Training)

بعد جمع البيانات، قم بتدريب النموذج:

```bash
python train_model.py
```

**ملاحظات:**
- سيستغرق التدريب عدة دقائق حسب عدد العينات
- سيتم حفظ النموذج في مجلد `models/`
- يمكنك مراقبة تقدم التدريب من خلال السجلات

### الخطوة 3: تشغيل التطبيق (Run Application)

بعد تدريب النموذج، شغل التطبيق:

```bash
python app/app.py
```

أو باستخدام الأمر المختصر (بعد التثبيت):
```bash
slp
```

---

## 🎮 دليل استخدام التطبيق

### الواجهة الرئيسية

**القسم الأيسر - عرض الفيديو:**
- عرض مباشر لتدفق الفيديو من الكاميرا
- رسم نقاط مفاصل اليد تلقائياً
- عرض الإشارة المكتشفة مع قيمة الثقة

**أزرار التحكم:**
- **Start** - بدء معالجة الفيديو
- **Pause** - إيقاف مؤقت
- **Stop** - إيقاف نهائي

**القسم الأيمن - التحكم والنتائج:**

**علامة التبويب Results:**
- عرض الإشارات المكتشفة
- نوع اليد (يمين/يسار)
- قيمة الثقة للتنبؤ

**علامة التبويب Settings:**
- اختيار رقم الكاميرا
- ضبط حد الثقة الأدنى (0.0 - 1.0)
- تحميل ملف فيديو
- حفظ الفيديو المعالج

---

## 📁 هيكل المشروع

```
SLP/
├── app/
│   ├── app.py                 # التطبيق الرئيسي PyQt5
│   └── camera_handler.py      # معالج الكاميرا والفيديو
├── src/
│   ├── mediapipe_processor.py # معالجة مفاصل اليد
│   ├── model.py               # نموذج الشبكة العصبية
│   ├── gesture_recognizer.py  # محرك التعرف الرئيسي
│   ├── data_collector.py      # أداة جمع البيانات
│   └── utils.py               # دوال مساعدة
├── data/                      # بيانات التدريب
├── models/                    # النماذج المدربة
├── config.py                  # إعدادات المشروع
├── collect_data.py            # سكريبت جمع البيانات
├── train_model.py             # سكريبت التدريب
├── requirements.txt           # المتطلبات
└── README.md                  # هذا الملف
```

---

## 🔧 التكوين والإعدادات

عدّل ملف `config.py` لتخصيص السلوك:

```python
# عدد الإشارات
GESTURES = ["hello", "thank_you", "yes", ...]

# إعدادات التدريب
TRAINING_CONFIG = {
    "epochs": 100,           # عدد جولات التدريب
    "batch_size": 32,        # حجم مجموعة البيانات
    "learning_rate": 0.001,  # معدل التعلم
}

# إعدادات الفيديو
VIDEO_CONFIG = {
    "frame_width": 1280,     # عرض الإطار
    "frame_height": 720,     # ارتفاع الإطار
    "fps": 30,              # معدل الإطارات
}
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: "لا توجد كاميرا"
**الحل:**
```python
# جرب تحديد رقم كاميرا مختلف
python -c "from app.camera_handler import CameraHandler; 
camera = CameraHandler(camera_index=1)"  # جرب 1 أو 2
```

### المشكلة: "لا توجد بيانات للتدريب"
**الحل:**
تأكد من جمع البيانات أولاً باستخدام:
```bash
python collect_data.py
```

### المشكلة: دقة منخفضة
**الحل:**
- زيادة عدد العينات لكل إشارة
- تحسين إضاءة الكاميرا
- التأكد من أن اليد واضحة ومرئية

---

## 📊 الأداء والدقة

### متطلبات النظام الموصى به

**للجمع والتدريب:**
- CPU: Intel i5 أو أفضل
- RAM: 8GB+
- GPU: اختياري لكن يسرع التدريب

**للتشغيل فقط:**
- CPU: Intel i3+
- RAM: 4GB+

### معدلات الدقة المتوقعة

- مع 100 عينة لكل إشارة: ~85-90%
- مع 200 عينة لكل إشارة: ~90-95%
- مع 500+ عينة لكل إشارة: ~95%+

---

## 🤝 المساهمة

نرحب بمساهماتك! يمكنك:

1. Fork المستودع
2. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى الفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

---

## 📝 الترخيص

هذا المشروع مرخص تحت MIT License - انظر ملف LICENSE للتفاصيل.

---

## 👥 الفريق

**فريق Sign Language Production**
- البريد الإلكتروني: slp@example.com
- الموقع: https://github.com/adelaboelkhair-cloud/SLP

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من قسم "حل المشاكل" أعلاه
2. افتح issue على GitHub
3. راجع الوثائق الكاملة

---

## 🚀 الخطوات التالية

بعد الانتهاء من التثبيت والتدريب:

- [ ] جمع بيانات إضافية لتحسين الدقة
- [ ] إضافة إشارات جديدة
- [ ] تحسين واجهة المستخدم
- [ ] دمج ميزات إضافية
- [ ] نشر التطبيق

---

شكراً لاستخدام Sign Language Production! 🎉
